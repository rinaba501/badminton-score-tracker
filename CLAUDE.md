# Badminton Score Tracker

A **watchOS app** built with SwiftUI for tracking badminton match scores in real time.

> **For the full feature specification, see [SPEC.md](SPEC.md).**
> CLAUDE.md covers project structure, architecture, and working conventions — kept deliberately terse; the deep rationale lives as doc comments in the source files themselves.
> SPEC.md covers what the app does and why.

## Tech Stack

- **Platform:** watchOS (scoring device) + iOS companion app in progress (#41, iPhone-only, iOS 17+) · **Language:** Swift · **UI:** SwiftUI
- **Shared code:** local Swift package `BadmintonCore` — Foundation-only (no SwiftUI/WatchKit) so both app targets and `swift test` on macOS consume it unchanged
- **Audio:** `AVAudioEngine` sine-wave tones + `AVSpeechSynthesizer` announcements — no audio files
- **Persistence:** `@AppStorage` (UserDefaults) with JSON-encoded structs
- **Sync:** Supabase/Postgres, opt-in per device (`SyncEngine` protocol; `SupabaseSyncEngine` on both targets, `NoOpSyncEngine` — pure local — is the default until a device explicitly signs in) — one row per match/player/club + a `settings` row (`SettingsSnapshot`: every scalar setting). Correctness is not CI-provable — needs a two-device real-account test
- **Health:** `HKWorkoutSession` + `HKLiveWorkoutBuilder` (HealthKit capability + usage descriptions in Info.plist)

## Project Structure

```
BadmintonCore/                 — local Swift package; platform-free core (no SwiftUI/WatchKit imports, ever)
  Sources/BadmintonCore/
    MatchModel.swift          — BadmintonMatch (pure scoring engine — no UI/timers/player identity), GameScore, MatchRecord, Side (live-match perspective, `.me`/`.opponent`). MatchRecord.winner is `RecordSide` (`.near`/`.far` — distinct from `Side`, a persisted viewer-neutral tag, not a display-name copy); MatchRecord has a custom Codable init that self-migrates legacy `winner: String` records without a PersistenceStore schema-version bump (Roadmap Phase 5a, #93). MatchRecord.clubId: UUID? tags a record as belonging to a Club — nil (default) means personal, unchanged (Roadmap Phase 5b). MatchRecord.isOfficial: Bool = true tags a match Official/Practice — false (Practice) is filtered out of Standings and the Pending Confirmation queue but still shows in the Activity Feed, set via PreMatchView's Official Match toggle
    PersistenceStore.swift    — all JSON encode/decode for [Player]/[MatchRecord]/[Club]/[ChallengeRecord]/[ReactionRecord]/[FriendRequest]/SettingsSnapshot; versioned envelope, per-record-tolerant decoding, migration hooks, plus single-record codecs + diff/conflict helpers for the sync layer (#109)
    SettingsSnapshot.swift    — every scalar setting synced as one `settings` row (). Custom Codable (decodeIfPresent-with-default) so fields added after the first records shipped still decode — same self-migration MatchRecord.winner uses. clubLastViewedActivity is merged per-club (max date) on apply, not overwritten (AppStore.applyRemoteSettings). The `accountLinked` field was removed entirely (a dead "link this device" opt-in nothing read anymore) — the same decodeIfPresent scheme that lets new fields join safely also lets a removed one leave safely, since Codable just ignores a JSON key the CodingKeys enum no longer declares. gameScreenStyle is iOS-only in practice (GameView.swift) but round-trips through Watch's AppStore/SupabaseSyncEngine like every other field, so it isn't dropped on sync. myName is the only identity string in the app (no separate Friends display name — see ClubDetailView/FriendsView). shareHistoryWithFriends (blind-overwritten) gates whether personal roster/history become RLS-visible to accepted friends — see FriendHistorySnapshot.swift. The five share*WithFriends toggles gate FriendIdentitySnapshot/FriendStatsSnapshot per field (avatar/gender/birthday/introduction all mirror on ANY one being on, since they share a single "FriendIdentity" record; stats mirrors separately) — name has no toggle, an accepted FriendRequest already carries a snapshotted displayName. gender/birthday/introduction are the personal data itself (locally-scoped like myName, only ever leave the device via FriendIdentitySnapshot)
    Player.swift              — Player model, SortOrder, sentinel identity (guest tokens vs. localized labels — see doc comments). Player.clubId: UUID? tags a roster entry as belonging to a Club — nil (default) means personal, unchanged (Roadmap Phase 5b)
    Club.swift                — Club model (id/name/createdDate, plus ownerRecordName: String? — Roadmap Phase 5c; nil means locally-owned, set when a club's zone is shared to us; requireMatchConfirmation: Bool? — Roadmap Phase 5 backlog #160, nil/false means off; seasonStartDate/seasonEndDate: Date? — Roadmap Phase 5 backlog #163, an optional time-boxed Standings window, nil start means no season set; trackStandings: Bool? — nil/true means ON, INVERTED nil-semantics vs. every other field here since existing clubs must keep showing Standings unchanged; false hides both the Standings and Season sections). Also hosts Club.isDateInSeason(_:), the shared "does this match's date fall in the club's season" predicate both ClubDetailViews call when filtering Standings only (never the Activity Feed). Grouping tag for Player/MatchRecord via clubId; membership lives in the `club_members` table (owner determined by this row's `owner_id`)
    ChallengeRecord.swift     — Roadmap Phase 5 backlog (#162): a "want to play?" ping between two club members, always tagged with a non-nil clubId. Unlike Club/Player/MatchRecord, fromParticipantId/toParticipantId identify real account holders (an `auth.uid()` string), not roster Players — a Player has no account link. Club-scoped only, no personal fallback (see AppStore.saveChallenges)
    ReactionRecord.swift      — Roadmap Phase 5 backlog (#164): an emoji reaction or one-line comment (kind + content) on a club match, authored by an account holder (same identity model as ChallengeRecord). Links to its match by plain matchId. Club-scoped only (see AppStore.saveReactions); orphaned reactions after a match/club delete stay invisible, never purged
    FriendProfile.swift       — Friends v1 (graph-only, club-independent — see ROADMAP.md "Friends" initiative): a discoverable `profiles`-table row so two people can find each other via an out-of-band invite link/code with no club in common yet. participantId is an `auth.uid()` string (the same identity space Challenge/ReactionRecord use once a device is Supabase-active). One profile per account, upserted by participantId. No local array cache — fetched on demand per participant
    FriendRequest.swift       — Friends v1: a friend request/graph edge between two people, independent of any Club. Structurally mirrors ChallengeRecord (fromParticipantId/toParticipantId/status/snapshotted display names) but has no clubId — lives in the `friend_requests` table, not a club. No separate Friendship record: an accepted FriendRequest *is* the friendship edge. Accepting never creates any extra sharing artifact by itself — history sharing is a separate, explicit opt-in (see FriendHistorySnapshot.swift)
    FriendHistorySnapshot.swift — a friend's shared personal (clubId == nil) roster + history — a friend's own players/match_records rows become visible via the `friend_can_view_history` RLS policy once they've turned on history sharing (SettingsSnapshot.shareHistoryWithFriends). Local-cache-only, keyed by participantId; never merged into AppStore.roster/history — someone else's data, shown read-only via AppStore.friendActivity
    FriendIdentitySnapshot.swift — a friend's shared profile identity (displayName always present; colorIndex/iconName/gender/birthday/introduction each independently nil-able per its own SettingsSnapshot toggle), mirrored as a single fixed "FriendIdentity" record in the same "FriendsHistory" zone. Local-cache-only (AppStore.friendIdentities), same never-merged contract as FriendHistorySnapshot
    FriendStatsSnapshot.swift — a friend's shared *derived* stats (gamesPlayed/wins/winRate/longestStreak/headToHead), gated by SettingsSnapshot.shareStatsWithFriends, mirrored as a single fixed "FriendStats" record — precomputed via FriendStatsSnapshot.compute(history:roster:) (reusing StatsCalculator) so stats can be shared without exposing raw match history. Local-cache-only (AppStore.friendStats), same never-merged contract as FriendHistorySnapshot
    FriendInviteLink.swift    — Friends v1 (7d): pure build/parse of the `badminton://addfriend?id=<participantId>&name=<name>` invite link (name trimmed + capped at 50 chars on both build AND parse — the link is editable UGC). Parsing never triggers a network write; consumption always goes through a confirmation sheet (iOS FriendInviteView)
    ClubInviteLink.swift      — Roadmap Phase 9d-2: pure build/parse of the `badminton://joinclub?id=<inviteId>&name=<clubName>` invite link, byte-identical shape to FriendInviteLink.swift (clubName trimmed + capped at 50 chars on both build AND parse). `inviteId` identifies a `club_invites` row (Supabase only — this is the Supabase-active alternative to CloudKit's CKShare invite, `redeem_club_invite` RPC does the actual join). Parsing never triggers a network write; consumption always goes through a confirmation sheet (iOS ClubInviteView, no Watch counterpart — same iOS-only-consumption precedent as FriendInviteLink, even though sending a link works on both targets via ShareLink)
    StatsCalculator.swift     — pure stats/history derivations; intentionally duplicated function pairs — see file header before unifying anything
    AppStorageKeys.swift      — single source of truth for EVERY UserDefaults/@AppStorage key string. `eraseAllDataResetKeys` (#264) is a curated array of every scalar setting key erased by AppStore.eraseAllData() — deliberately excludes content-array keys (cleared via their own AppStore.save* diffing instead), Supabase transport/identity bookkeeping (supabaseAccountLinked/myParticipantId), and the StoreKit entitlement cache
    Entitlements.swift        — monetization: ProductID constants + pure owned-product-IDs → features mapping (isPro/hasAllThemes/hasAllAvatars/showsAds; packs count as owned under Pro). StoreKit-free by design — each target's StoreManager feeds it; purchase state is per-Apple-ID via StoreKit, NEVER synced to the backend (ownedProductIds key is a local launch cache only)
    ScoreCallFormatter.swift  — locale-aware spoken score formatting (en/ja/zh); injectable strings closure for tests
    SyncEngine.swift          — `SyncEngine` protocol capturing the outbound half of AppStore's sync boundary (every `enqueue*`/Friends-teardown method AppStore calls to push a local change out) — `SupabaseSyncEngine` (both targets) is its real conformer; `NoOpSyncEngine` is the local-only default for a device that has never signed in (`AppStore.shared`'s default, set at `static let shared`). The reverse direction (a backend calling AppStore.applyRemote* when remote data arrives) stays outside the protocol — AppStore is a concrete singleton any backend calls into directly. `removeFriendStatsRecord()` mirrors `removeFriendIdentityRecord()`'s shape
  Tests/BadmintonCoreTests/   — all core unit tests; run with `swift test --package-path BadmintonCore`

CloudSyncSpike/                — local Swift package: production Supabase transport for Roadmap Phase 9 (backend migration to Postgres/Supabase, see docs/supabase-migration-plan.md — started life as a DEBUG-only feasibility spike, promoted to real code in Phase 9c). `SupabaseConfig` holds the real project URL + anon key (hardcoded — Supabase anon keys are designed to be client-embeddable, RLS-gated, not secret). `SupabaseSyncManager` wraps supabase-swift: Google OAuth (iOS, `ASWebAuthenticationSession`, relayed to the watch over WCSession via WatchAppDelegate/AppDelegate since watchOS has no in-app browser) plus typed CRUD against `players`/`match_records`/`settings` (the Phase 9c tier) and, since Phase 9d, `clubs`/`challenges`/`reactions` — push (`upsert*`/`delete*`, via `PendingRecord` for players/match_records or `ClubPendingRecord`/`ChallengePendingRecord`/`ReactionPendingRecord` for the three tables whose columns don't fit that shape — `clubs` has no `club_id`, `challenges`/`reactions` have no single `owner_id`) and the pull-side counterpart (`fetchAllRows`/`fetchSettings` for a one-time catch-up read, `startRealtimeSync`/`stopRealtimeSync` for ongoing Postgres Changes delivery via `RealtimeChannelV2`). `startRealtimeSync`/`fetchAllRows` apply NO client-side `owner_id` filter (removed in 9d) — delivery is scoped by RLS alone, since `challenges`/`reactions` have no owner_id column to filter on, and a uniform owner-only filter would incorrectly drop legitimate updates from other members of a shared club. `RemoteChange` is the normalized shape both the catch-up read and the realtime stream hand back (table/id/kind/raw payload `Data`/`ownerId`/`clubId`) — `ownerId` exists so a receiving device can backfill `Club.ownerRecordName` from the row's real `owner_id` column rather than trusting the payload's own embedded value (which the owner always encodes as `nil`); `clubId` (Roadmap Phase 9e-3) exists so a `.delete` event — which carries no `payload` to decode a model's own `clubId` from — can still be routed correctly by `SupabaseSyncEngine`, backfilled from the old-row image `players`/`match_records`' existing `REPLICA IDENTITY FULL` (9c-5) already provides. Decoding `payload` into a concrete model and applying it to `AppStore` is left to each target's `SupabaseSyncEngine`, not this package. Roadmap Phase 9d-2 adds `createClubInvite`/`redeemClubInvite` (the CKShare-invite alternative from CloudKit's era, which has since been fully removed, Phase 9f-3): `createClubInvite` (owner-only, RLS-enforced) inserts a `club_invites` row and returns its id for building a `ClubInviteLink` URL; `redeemClubInvite` wraps the `redeem_club_invite` `SECURITY DEFINER` RPC (`supabase/schema.sql`), which validates expiry/max_uses server-side and inserts the caller into `club_members` — `club_members` has no direct INSERT policy, so this RPC is the only way to join a club you don't own. Roadmap Phase 9d-3 adds `fetchClubMembers(clubId:)` (two client-side queries — `club_members` then `profiles` filtered by the resulting ids, not a PostgREST embed, since neither table references the other), `leaveClub`/`removeMember` (plain deletes, covered by the existing `club_members_delete` RLS — self or owner, no new SQL), and `upsertMyProfile(displayName:)` — `profiles` was left unpopulated through 9d-1/9d-2 per 9a's own schema comment deferring it to 9e, so this narrow addition populates just the caller's own row, called from each target's `SupabaseSyncEngine.startIfActive()`. Roadmap Phase 9e-1 adds `fetchProfileDisplayName`/`sendFriendRequest`/`respondToFriendRequest` against `profiles`/`friend_requests` (both reused from 9a/9d-3, no new tables) — all primitives-only (`UUID`/`String`/`Data`) since this package still has no `BadmintonCore` dependency and so can't build a `FriendRequest`/`FriendProfile` itself; each target's View builds the model, encodes via `PersistenceStore`, and passes the pieces in. No new pull method: `friend_requests` is id-keyed like every other table, so it reuses `fetchAllRows`/`startRealtimeSync` unchanged, just added to `pullTables`. Roadmap Phase 9e-2 adds `upsertFriendIdentitySnapshot`/`removeFriendIdentitySnapshot`/`upsertFriendStatsSnapshot`/`removeFriendStatsSnapshot` against `friend_identity_snapshots`/`friend_stats_snapshots` (new, `id`+`payload jsonb` like every other table — deliberately not RLS-granted directly on `settings`, whose single jsonb blob holds unrelated scalars alongside the shareable fields — reuse `fetchAllRows`/Realtime unchanged). Roadmap Phase 9e-4 adds `deleteMyProfile()`/`deleteAllFriendRequests()` (the Erase-All-My-Data teardown, `AppStore.eraseAllData()`'s call sites) — `deleteMyProfile()` needed a new `profiles_delete` RLS policy (`supabase/schema.sql`, 9a never added one); `deleteAllFriendRequests()` deletes every `friend_requests` row the caller is party to via an explicit `.or(...)` filter on `from_participant_id`/`to_participant_id`, matching a "delete every row I'm party to" bidirectional intent even though RLS alone already scopes it correctly. Deliberately does NOT conform to `SyncEngine` itself — it has no dependency on `AppStore` (an app-target type a shared package can't import); each target's own `SupabaseSyncEngine.swift` is the thin per-target adapter that does conform, reading `AppStore.shared`'s live roster/history/settings/clubs/challenges/reactions by id and calling into this manager

badminton score tracker Watch App/
  ContentView.swift          — root view; owns the AppView routing enum (.menu/.preMatch/.game/.settings/.history/.stats — state-driven, no top-level NavigationLink) and a first-launch, skippable "what should we call you?" prompt (AppStorageKeys.didPromptForName, shown once while myName is still the Player.defaultMyName placeholder) — keeps the local-first "always works with zero setup" invariant by defaulting to Skip-able rather than blocking
  MenuView.swift             — main menu
  PreMatchView.swift         — player selection: 2 steps (Singles) or 4 steps (Doubles, reading SettingsView.GameMode); near-side step also offers a Club picker (default Personal, hidden if the user has no clubs — Phase 5 backlog, #169) whose selection is written to matchClubId and read by GameViewModel.saveMatch(). When the selected club tracks standings, an Official Match Toggle (default on) also appears, writing matchIsOfficial (AppStorageKeys.matchIsOfficial) — reset to true whenever no club is selected or the selected club has standings tracking off
  GameView.swift             — live scoring screen, layout only; all logic delegates to GameViewModel. ScoreView renders one or two stacked names per team depending on whether a partner is present. Toolbar's second button (gear) presents InMatchSettingsView (#260)
  GameViewModel.swift        — @MainActor ObservableObject; owns all live-game logic: scoring, undo, time mode, haptics, persistence, announcements, doubles partner names/rotation display
  HapticsProvider.swift      — HapticsProvider protocol + Watch/NoOp implementations (tests use NoOp)
  StoreManager.swift         — StoreKit 2 plumbing (Transaction.updates listener, purchase/restore, entitlement refresh + UserDefaults cache); per-target copy pattern like HapticsProvider. Repo-root Badminton.storekit is the local-testing product config
  PaywallView.swift          — slim watch storefront (products + restore), presented as a sheet from Settings and every lock badge (StatsView lock rows, theme picker, PlayerEditView avatar grid)
  InMatchSettingsView.swift  — #260: compact sheet presented from GameView's toolbar gear button, exposing only the settings already read live mid-match (Crown Scoring, Sound Effects, Score Announcement, Court Theme via the shared CourtThemeSection) — Match Format/Timer/Court Change Reminders stay full-Settings-only
  EraseDataView.swift        — #264: pushed from SettingsView's Danger Zone section, receives the same `currentView` binding SettingsView has. Deletes every local + Supabase-synced record the account owns via AppStore.eraseAllData(), gated behind a type-to-confirm `TextField` (fixed, intentionally non-localized keyword "DELETE" — same convention as GitHub/AWS-style destructive confirmations). Any club the user owns (`ownerRecordName == nil`) is listed by name and deleted outright as part of the erase — no in-app ownership-transfer path exists (#268 backlog). On success sets `currentView = .menu`, which re-triggers ContentView's first-launch name prompt since myName/didPromptForName are among the reset keys
  SettingsView.swift         — match format, audio, theme, timer, roster management, club management entry point (Phase 5d), Friends entry point (Phase 7e), a "Sync Backend" section (Roadmap Phase 9c-3) that lets this device opt personal data (Settings + personal Player/MatchRecord only) into Supabase — signed in via a relayed `SupabaseSyncManager` session (this device never performs Google OAuth itself, see `WatchAppDelegate`), the toggle itself just calls `AppStore.shared.activateSupabaseSync()`/`deactivateSupabaseSync()` and writes `AppStorageKeys.supabaseAccountLinked` — then Danger Zone section (#264, EraseDataView.swift)
  HistoryView.swift          — saved match list + filters, incl. a club scope filter (Personal + joined clubs, Roadmap Phase 5f; sheet-based since `Menu` is unavailable on watchOS) that pre-filters by `clubId` before any StatsCalculator call
  StatsView.swift            — per-player stats + head-to-head (math lives in StatsCalculator); same club scope filter as HistoryView (Phase 5f), via an inline `Picker`
  PlayerEditView.swift       — single-player editor sheet; Phase 5d adds an optional club Picker (bound to Player.clubId) shown only when the caller passes a non-empty `clubs` list
  ClubsView.swift            — Phase 5d: club list + create, entered via a SettingsView section; pure UI over AppStore.saveClubs
  FriendsView.swift          — Friends v1 (Phase 7e): incoming/outgoing pending FriendRequests, the friends list (AppStore.friends), and an add-friend ShareLink over FriendInviteLink.url — works on watchOS since a friend invite is just a URL, same shape as the Club invite link. Friends always shows/sends the local scoring name (myName, via Player.displayName(for:)) — no separate Friends display name to prompt for. Every name row gets an AvatarView (roster-matched names get the real avatar via avatarColor(for:)/avatarIcon(for:), the same lookup PreMatchView/ClubDetailView use; requests/friends fall back to flat gray since those aren't local Player records with a saved color/icon). Backstop for anyone who skipped ContentView's first-launch prompt: if myName is still the default placeholder, refresh() opens a one-time-per-visit name prompt instead of publishing a profile, and sendCode() pauses behind the same prompt (via a stashed pendingAction closure) rather than send an unset identity. Phase 7f adds a second "Enter a Code" row: pastes either a full invite link (parsed via FriendInviteLink.parse) or a bare participantId. A trailing "More" section holds two NavigationLinks — "Friend Activity" (FriendActivityView) and "Sharing Settings" (FriendSharingSettingsView) — deliberately last since neither is a day-to-day action. Entered via SettingsView. Talks to Supabase throughout (SupabaseSyncManager/SupabaseSyncEngine) — Roadmap Phase 9f-2 removed the original CloudKit-branched code this shipped with (7a-7d/9e-1), including the "Account" link/unlink section (a metadata-only `SettingsSnapshot.accountLinked` opt-in that had no display effect and nothing left reading it once CloudKit's own sync engine stopped starting at launch, Phase 9f-1)
  FriendSharingSettingsView.swift — pushed from FriendsView's "Sharing Settings" row: the six independent per-field toggles (avatar/gender/birthday/introduction/stats/history, each own SettingsSnapshot Bool) that replaced the old single "Share My History with Friends" switch — split onto its own screen because these are a set-once-and-forget decision, not something that belongs above the friends list. Each toggle turning on/off calls `AppStore.shared.enqueueSettingsChange()`, plus `AppStore.shared.syncEngine.enqueueFriendStatsChange()`/`removeFriendStatsRecord()` for the stats toggle and `refreshMyIdentitySnapshotIfSharing()` for the four identity toggles — the settings write alone is the complete access change, friend visibility is RLS + the toggle itself
  FriendActivityView.swift   — a friend list (union of AppStore.friendActivity/friendIdentities/friendStats keys — a friend who's shared only stats still shows up) pushes a per-friend profile screen: avatar/gender/birthday/introduction header (whichever fields that friend shared), a Stats section (win rate/games/head-to-head), and a Match History section reusing HistoryView's MatchHistoryRow verbatim — each section only appears if that friend shared it
  ClubDetailView.swift       — Phase 5d: rename, member list (read via `SupabaseSyncManager.fetchClubMembers`, `club_members` joined against `profiles`; falls back to a self-row-only view when not signed in), and per-club roster (players filtered by clubId) for one Club; delete/leave clears clubId back to nil on that club's players/matches (never deletes them) then removes the club via the existing saveClubs diffing — a non-owner's "leave" also explicitly calls `SupabaseSyncManager.leaveClub` since the owner-only `clubs_delete` RLS means the diffing's implicit delete alone silently no-ops for a non-owner. Activity rows show the exact per-game score (StatsCalculator.ActivityFeedEntry.games, same "21-18, 15-21" format HistoryView's gameLine uses — not just the games-won tally), a reaction-count caption, and push MatchReactionsView (#164, which shows the same exact-score line). The Members "you" row, any Standings entry matching myName, and either side of an Activity row's "X vs Y" matching myName all get the same small checkmark-seal badge (youBadge, accessibility-labeled via the "clubs.you" string) instead of a hardcoded "You" label, so your own name always reads like every other name in the list — an Activity entry's myName is whoever recorded that match, so the badge can land on either side depending on who scored it. Members/Standings/Activity rows all get an AvatarView per name, using the avatarColor(for:)/avatarIcon(for:) roster-name lookup (Standings/Activity names are real roster Players who've played matches, and "you" always has a roster row once "Me" has been customized in Settings) with a flat-gray fallback for remote Members we have no local roster match for. Each fetched member also gets a friend badge (`ClubParticipant.isFriend`, currently always hardcoded `false` — a known gap, not wired up to cross-reference `AppStore.friends` yet). Same first-launch-skip backstop as FriendsView: onAppear opens a passive, non-blocking name prompt if myName is still the default. Standings section footer shows a read-only "Season: <start> – <end/present>" label (Club.isDateInSeason(_:)-filtered) when the club owner has set a season (#163) — no editing UI on Watch. A "Track Standings" Toggle sits next to Require Match Confirmation in the owner-gated name Section; turning it off hides the whole Standings section (Club.trackStandings). Activity rows show a small "Practice" text tag next to the score line when entry.isOfficial is false; Standings/Pending Confirmation both filter out practice matches too. Both the owner-gated Invite row (`prepareInvite`, calls `SupabaseSyncManager.createClubInvite` and presents a `ShareLink` over the resulting `ClubInviteLink` URL — the Watch's first invite-sending affordance) and the Members row's owner-only swipe-to-kick action (`removeMember`) are gated on `supabaseAccountLinked` (signed in), the only Watch invite/kick flow that has ever existed here
  MatchReactionsView.swift   — Phase 5 backlog (#164): emoji toggling + read-only comment list for one club match, pushed from a ClubDetailView activity row (comment authoring is iOS-only — no Watch keyboard)
  PlayerAvatar.swift         — app-side presentation extension of Player (colors/images/icons + AvatarView)
  AudioFeedback.swift        — ScoreAnnouncer (speech) + SoundPlayer (tones)
  CourtTheme.swift           — CourtTheme enum, plus CourtThemeSection (#260, shared inline Picker + paywall-snap-back logic used by both SettingsView and InMatchSettingsView)
  AppStore.swift             — @MainActor singleton; caches decoded roster/history/clubs, runs migrations on init, all writes go through it and enqueue to an injected `syncEngine: SyncEngine` (set to `SupabaseSyncEngine.shared` or `NoOpSyncEngine()` at `static let shared`, chosen by reading `AppStorageKeys.supabaseAccountLinked` so a relaunch stays on whichever backend was last active — see `SyncEngine.swift`). Mutable — `activateSupabaseSync()` (no-ops unless `SupabaseSyncManager.shared.isSignedIn`, then swaps `syncEngine`, re-enqueues every existing roster/history/settings id as a one-time migration upload, and calls `SupabaseSyncEngine.shared.startIfActive()` to pull-and-subscribe for anything already on the remote account) and `deactivateSupabaseSync()` (stops the Realtime subscription via `SupabaseSyncEngine.shared.stopRealtimeSync()`, then reverts to local-only `NoOpSyncEngine`; no remote delete either way) are the only two call sites allowed to reassign it; neither touches the `supabaseAccountLinked` flag itself, that's the caller's job (a `@AppStorage`-bound Settings toggle). `currentSettingsSnapshot()` also lives here since `SupabaseSyncEngine` needs the scattered-`@AppStorage`-scalars snapshot logic — one copy per target. Owns localPlayerId ("Me" is never in the roster). saveRoster/saveHistory/clearHistory also enqueue the Settings record; applyRemoteSettings writes every scalar (ignores empty localPlayerId). Friends v1 (#7c): also caches friendRequests + a derived `friends` computed property (accepted requests only); saveFriendRequests is the one save* method that does NOT go through `syncEngine`'s enqueue* path — it only reconciles the local cache after a direct write or a `refreshFriendRequests()` poll/Realtime event, and also prunes `friendActivity`/`friendIdentities`/`friendStats` entries for anyone no longer an accepted friend. `friendActivity: [String: FriendHistorySnapshot]`/`friendIdentities: [String: FriendIdentitySnapshot]`/`friendStats: [String: FriendStatsSnapshot]` (all keyed by participantId) are separate caches for friends' shared data, populated by `applyRemoteFriendActivity`/`applyRemoteFriendIdentity`/`applyRemoteFriendStats` (+ matching `*Deletion(s)` methods, called from `SupabaseSyncEngine`) — deliberately never merged into `roster`/`history`. `eraseAllData()` (#264) is the one full-wipe orchestrator: calls the existing save*/clearHistory methods with empty state, then the `SyncEngine`'s three Friends-teardown methods (`deleteAllMyFriendRequests`/`deleteMyFriendProfile`/`deleteFriendsHistoryZone` — the last a documented permanent no-op under Supabase, see `SupabaseSyncEngine`'s entry), then resets every key in `AppStorageKeys.eraseAllDataResetKeys` and re-enqueues Settings, plus explicit, unconditional `syncEngine.removeFriendIdentityRecord()`/`removeFriendStatsRecord()` calls rather than relying on `saveRoster`'s roster-diff-gated `refreshMyIdentitySnapshotIfSharing()` path to happen to fire — the only thing that reliably clears the `friend_identity_snapshots`/`friend_stats_snapshots` rows, which have no bulk-delete-by-owner shortcut
  WorkoutManager.swift       — HKWorkoutSession lifecycle (start on match begin, end on save/discard)
  SupabaseSyncEngine.swift   — the `SyncEngine` conformer `AppStore` swaps to when a device opts into Supabase. Thin adapter over `CloudSyncSpike`'s `SupabaseSyncManager` — reads `AppStore.shared`'s live roster/history by id and re-encodes via `PersistenceStore` (settings snapshot itself lives on `AppStore.currentSettingsSnapshot()`), materializing each record fresh from the live cache rather than caching a copy of its own. Every `enqueue*` chains through a private serial `Task` (`pendingWork`) rather than spawning an independent one, so two calls in quick succession apply in order instead of racing; roster/history upserts batch into one `SupabaseSyncManager.upsertPlayers`/`upsertMatchRecords` call (built around `PendingRecord`, a named struct — a plain tuple tripped SwiftLint's `large_tuple` rule at 4 members) instead of looping one request per record. Every table is real: personal-data tier, clubs/challenges/reactions, and the Friends graph (requests, identity/stats sharing, history sharing) — `enqueueFriendsRosterChanges`/`enqueueFriendsHistoryChanges` stay permanent no-ops by design (a personal record already pushes unconditionally elsewhere; friend visibility is RLS-gated on that same row, no mirrored copy needed). Pull-side: `startIfActive()` (called from `AppStore.activateSupabaseSync()` after the migration-on-signin push, and unconditionally-but-cheaply at app launch, gated there on `AppStorageKeys.supabaseAccountLinked`) runs a one-time catch-up read (`SupabaseSyncManager.fetchAllRows`/`fetchSettings`) then opens a Realtime subscription (`startRealtimeSync`); `handleRemoteChange` decodes each `RemoteChange`'s payload via `PersistenceStore` and applies it through `AppStore.applyRemoteUpsert`/`applyRemoteDeletions`/`applyRemoteSettings`, so a Realtime event and the catch-up read land through one shared apply path. Deliberately does NOT gate on `SupabaseSyncManager.isSignedIn` (only set by an explicit sign-in call, so it's still false on a cold relaunch even though the SDK auto-restores a persisted session) — each downstream call independently no-ops if `currentUserId()` is nil. `stopRealtimeSync()` is called from `deactivateSupabaseSync()`. `ownerRecordName(for:)` backfills `Club.ownerRecordName` from `RemoteChange.ownerId` rather than trusting the payload; `enqueueClubChanges` skips any club this device doesn't own, since `clubs_update`/`clubs_delete` RLS is owner-only so a non-owner write would silently no-op anyway; `ChallengeRecord`/`ReactionRecord`'s opaque participant-id `String` fields hold this account's `auth.uid()` string, parsed to `UUID` for the row's participant columns, same opaque-backend-id pattern `Club.ownerRecordName` uses. `startIfActive()` also calls `SupabaseSyncManager.upsertMyProfile(displayName:)` on every activation/launch so `ClubDetailView`'s member list has a display name to show, and caches `currentUserId()` into `AppStorageKeys.myParticipantId` — `AppStore.friends` reads that key directly (not through `SyncEngine`), so without this call it would never get populated and `AppStore.friends` would silently stay empty forever; `friend_requests` is in `pullTables`, with `refreshFriendRequests()` doing a full refetch-and-reconcile into `AppStore.saveFriendRequests` (not a per-id merge) on the initial pull and on every Realtime event for that table regardless of kind. `enqueueFriendIdentityChange`/`removeFriendIdentityRecord`/`enqueueFriendStatsChange`/`removeFriendStatsRecord` push per-field-toggle-gated snapshots to `friend_identity_snapshots`/`friend_stats_snapshots`; pull/Realtime handling for those two tables resolves a display name from the already-synced `AppStore.friends` (they carry none of their own) and filters out a device's own row (`id == currentUserId()`) before applying it — RLS legitimately also returns the caller's own row, so without the filter a user would see themselves as their own friend. `pullInitialState()`/`handleRemoteChange()`'s `players`/`match_records` handling routes through `isPersonalOrClubRow(clubId:ownerId:myId:)` — `clubId != nil` (club-shared) or `ownerId == nil`/`== currentUserId()` (personal, including the push-echo case) goes through `applyRemoteUpsert`/`applyRemoteDeletions` unchanged; anything else is a friend's row newly visible via `friend_can_view_history` RLS (`supabase/schema.sql`) and routes to `AppStore.applyRemoteFriendActivity`/`applyRemoteFriendActivityDeletions` instead, so it's never merged into this device's own roster/history. `deleteMyFriendProfile()`/`deleteAllMyFriendRequests()` call `SupabaseSyncManager.deleteMyProfile()`/`deleteAllFriendRequests()` (see `CloudSyncSpike`'s entry above); `deleteFriendsHistoryZone()` stays a documented permanent no-op — no zone concept under Supabase, friend history access is pure RLS gated by `shareHistoryWithFriends` over rows that same method's `saveRoster([])`/`clearHistory()` delete outright
  badminton_score_trackerApp.swift — entry point; handles badminton://newmatch deep link. Installs WatchAppDelegate via @WKApplicationDelegateAdaptor. If AppStorageKeys.supabaseAccountLinked is true, calls SupabaseSyncEngine.shared.startIfActive() to reconnect Realtime + catch up on launch — a no-op check for the common not-yet-signed-in device; an unlinked device stays local-only (AppStore.shared defaults to NoOpSyncEngine) until an explicit Supabase sign-in. iOS's own entry point (badminton score tracker/badminton_score_trackerApp.swift) mirrors this
  WatchAppDelegate.swift     — WKApplicationDelegate. Activates WCSession to receive a Supabase session relayed from the iPhone (watchOS has no in-app browser, so it never performs Google OAuth itself)
  Assets.xcassets/           — app icon, racket animation, 15 avatar images
  *.lproj/Localizable.strings — en, ja, zh-Hans, ko, id, hi

badminton score tracker Watch AppTests/  — GameViewModel tests (compiled in CI, run locally on a simulator)
badminton score tracker Complication/    — WidgetKit extension (circular/corner/inline/rectangular)

badminton score tracker/       — iOS companion app (#41, in progress). Same target that used to be the watch-only stub container — now a real iOS app (NavigationStack root, iPhone-only, iOS 17+) that still embeds the Watch App. Has its own Info.plist, Assets.xcassets, .entitlements, and 6 *.lproj tables (iOS-only chrome under ios.*; History/Stats screens reuse the Watch's own string keys verbatim so translations copy across); the Watch App's Info.plist now declares it as companion (WKCompanionAppBundleIdentifier) instead of WKWatchOnly
  ContentView.swift          — iOS root: NavigationStack + List menu linking to Profile, History, Stats, and Friends (a numeric badge shows the incoming pending-request count, from a cached myParticipantId lookup mirroring AppStore.friends). Also owns onOpenURL for the `badminton://addfriend` invite link (scheme registered in this target's Info.plist, Roadmap 7d), presenting FriendInviteView as a sheet, and the same first-launch skippable name prompt as the Watch's. Roadmap Phase 9d-2: onOpenURL also tries `ClubInviteLink.parse` when `FriendInviteLink.parse` misses, presenting ClubInviteView as a sheet — the two invite links share one scheme (`badminton://`) but different hosts (`addfriend`/`joinclub`), so parsing is chained rather than branched on host up front
  ProfileView.swift          — iOS-only: dedicated "who am I" page, split out from RosterView (name/avatar editing for "Me", reusing PlayerEditView) and FriendsView (the friend graph). Also owns editing of the identity fields (gender Picker, birthday DatePicker, introduction TextEditor capped at 200 chars like MatchReactionsView's comment) — these aren't @AppStorage-native types (String?/Date?) so they're loaded on appear and written straight to UserDefaults + `AppStore.shared.enqueueSettingsChange()` + `AppStore.refreshMyIdentitySnapshotIfSharing()`. Each of the four identity fields (avatar/gender/birthday/bio) has its own inline icon-only `Toggle(.toggleStyle(.button))` next to it (the `shareToggle` helper) gating that field's SettingsSnapshot share Bool, so the sharing decision surfaces at the point of editing, not just on FriendsView's Sharing Settings screen; since the icon carries no visible text, toggling one shows a brief self-dismissing toast (`showToast`/`toastMessage`) confirming what just happened. `toggleIdentityField` here is a deliberate duplicate of FriendSharingSettingsView's — the two per-field toggles are two entry points to the same four Bools, kept in sync by hand. No Watch counterpart for editing (no keyboard/DatePicker-equivalent flow there, same iOS-only precedent as MatchReactionsView's comment composer) — the small screen keeps "Me" as a distinguished row inside Settings/Roster there instead of a separate nav destination. Roadmap Phase 9f-2 removed the CloudKit-era "Account" link/unlink section (a dead `SettingsSnapshot.accountLinked` metadata flag with no display effect) and, with it, this view's only reason to show its own name-prompt sheet — editing "Me" directly at the top of this page is now the sole way to set a name here
  FriendInviteView.swift     — Friends v1 (7d), iOS-only: confirmation sheet for a parsed FriendInviteLink. No Watch counterpart — the invite *link* stays iOS-only, unlike FriendsView itself (Roadmap 7e). send() trusts invite.participantId as a real auth.uid() string and invite.displayName as-is (both UGC from the link itself), builds the FriendRequest locally, and calls SupabaseSyncManager.sendFriendRequest/SupabaseSyncEngine.refreshFriendRequests — Roadmap Phase 9f-2 removed the original CloudKit branch (9e-1) this shipped with
  ClubInviteView.swift       — Roadmap Phase 9d-2, iOS-only: confirmation sheet for a parsed ClubInviteLink, byte-shape mirror of FriendInviteView; confirm calls `SupabaseSyncManager.redeemClubInvite(inviteId:)` (wraps the `redeem_club_invite` RPC) then `SupabaseSyncEngine.shared.startIfActive()` so the newly-joined club's data appears immediately rather than waiting for the next launch or Realtime event. No Watch counterpart, same precedent as FriendInviteView
  FriendsView.swift          — Friends v1 (Phase 7e): incoming/outgoing pending FriendRequests, the friends list (AppStore.friends), and an add-friend ShareLink over FriendInviteLink.url. iOS restyle of the Watch's — ShareLink works on watchOS too (a friend invite is just a URL), same shape as ClubDetailView's invite, so this view is symmetric across targets. Same first-launch-skip backstop name prompt as the Watch's (refresh()/sendCode() pause behind it while myName is still the default placeholder). Phase 7f adds a second "Enter a Code" row: pastes either a full invite link (parsed via FriendInviteLink.parse) or a bare participantId. Same trailing "More" section (Friend Activity + Sharing Settings NavigationLinks) as the Watch's. Talks to Supabase throughout — Roadmap Phase 9f-2 removed the original CloudKit-branched code this shipped with (7a-7d/9e-1), including the account link/unlink toggle that used to live in ProfileView (a dead metadata flag, nothing read it)
  FriendSharingSettingsView.swift — iOS restyle of the Watch's: the six per-field sharing toggles (avatar/gender/birthday/introduction/stats/history), pushed from FriendsView's "Sharing Settings" row rather than shown inline
  FriendActivityView.swift   — iOS restyle of the Watch's friend-profile view (push-based NavigationStack instead of a sheet, otherwise identical) — friend list is the union of friendActivity/friendIdentities/friendStats keys, detail screen shows whichever of identity/stats/history sections that friend actually shared; reuses this target's own MatchHistoryRow
  HistoryView.swift          — iOS match list + MatchHistoryRow; date/type/sort/player filters, a toolbar club-scope `Menu` (Personal + joined clubs, Roadmap Phase 5f), and swipe-delete/clear-all. All filtering delegates to StatsCalculator; pushed via NavigationStack (no currentView binding). iOS-native restyle of the Watch's HistoryView. Personal scope (selectedClubId == nil) also shows an inline "Share My History with Friends" Toggle (visible-text, unlike ProfileView's icon-only ones — history is a bigger reveal) at the top of the list; `toggleShareHistoryWithFriends` here is a deliberate duplicate of FriendSharingSettingsView's
  StatsView.swift            — iOS per-player stats: win-rate ring + stat-card grid + avatar'd head-to-head; math all in StatsCalculator; same toolbar club-scope `Menu` as HistoryView (Phase 5f). iOS-native restyle of the Watch's StatsView. When viewing your own personal stats (selectedClubId == nil && activePlayer == myName — the only case shareStatsWithFriends applies to), the header carries an icon-only inline share toggle + confirmation toast, same ProfileView pattern; `toggleStatsSharing` here is a deliberate duplicate of FriendSharingSettingsView's
  RosterView.swift           — iOS roster management for other players: add/rename/delete players, sort order. Ported from the Watch SettingsView roster section, incl. the rename→history propagation (renames update past matches via player id). Writes via AppStore.saveRoster/saveHistory. "Me" editing lives in ProfileView, not here
  PlayerEditView.swift       — iOS single-player editor sheet (real keyboard TextField + duplicate detection, color/avatar/icon grids). Restyle of the Watch's; validation logic verbatim. Phase 5d adds the same optional club Picker as the Watch's
  ClubsView.swift            — iOS restyle of the Watch's Phase 5d club list + create; entered from ContentView's menu (not SettingsView, matching where RosterView is entered)
  ClubDetailView.swift       — iOS restyle of the Watch's Phase 5d club detail (rename, member list, per-club roster, delete/leave). Activity rows carry inline emoji reaction chips + a comment-count button opening MatchReactionsView (#164). Same badge-based "you" row (Members, Standings, and Activity rows), avatarColor(for:)/avatarIcon(for:)-backed AvatarView, per-member friend badge (currently always `false`, same known gap as the Watch's), and first-launch-skip backstop name prompt as the Watch's. An owner-only "Season" section (Toggle + DatePicker for a start date, Toggle + DatePicker for an optional end date) lets the club owner time-box Standings via Club.seasonStartDate/seasonEndDate (#163); the Standings section footer shows the same read-only label non-owners/Watch see. The Activity Feed is deliberately untouched by season filtering. A "Track Standings" Toggle sits next to Require Match Confirmation in the name Section (both owner-gated); turning it off wraps and hides both the Season section and the Standings section (Club.trackStandings). Activity rows show a small capsule "Practice" tag next to the score line when entry.isOfficial is false; Standings/Pending Confirmation both filter out practice matches too. The owner-only Invite button (gated on `supabaseAccountLinked`) calls `SupabaseSyncManager.createClubInvite` and presents a `ShareLink` over the resulting `ClubInviteLink` URL (`clubInviteBox`/`ClubInviteShareSheet`); `loadParticipants()` reads `SupabaseSyncManager.fetchClubMembers(clubId:)`; the Members row gets an owner-only `.swipeActions` kick (`removeMember`); `removeClub()`'s non-owner "leave" path also explicitly calls `SupabaseSyncManager.leaveClub` alongside the existing `saveClubs` diffing, since the owner-only `clubs_delete` RLS means that diffing's implicit delete alone silently no-ops for a non-owner (the owner's real delete still cascades via the FK, unchanged)
  MatchReactionsView.swift   — Phase 5 backlog (#164), iOS: reactions/comments sheet for one club match — emoji toggling plus comment composing (TextField + Send, trimmed, 200-char cap); ReactionEmojiButton is the shared borderless chip also used inline on ClubDetailView's activity rows
  PlayerAvatar.swift         — iOS presentation extension of Player (avatarColors/avatarImageNames/sportIcons/AvatarView, plus guestAvatarColors — a fixed, colorblind-safe palette index-aligned with Player.guestTokens). Per-target copy of the Watch's; the 15 avatar images are duplicated into this target's Assets.xcassets
  ShareCard.swift            — iOS-only (#13): match-result card rendered to PNG via ImageRenderer + a SharableMatchCard Transferable (image + plain-text). Long-press a history row → ShareLink. No Watch counterpart
  NewMatchFlow.swift         — modal container (fullScreenCover from ContentView) routing preMatch → game; PreMatchView writes match-config @AppStorage, GameView's VM reads it on appear (same handoff as the Watch's .preMatch/.game routes)
  PreMatchView.swift         — iOS player selection: near/far Step, each with a player/partner SubStep sharing one screen — picking the side's player swaps the same list in place to prompt for their partner (summary banner shows the already-picked player, tap to change) instead of pushing a new screen like the Watch's 4-step flow does, h2h records; reuses PlayerEditView to add players. Closure-driven (onReady/onCancel), no currentView binding. Near-side step also offers a Club picker (default Personal, hidden if the user has no clubs — Phase 5 backlog, #169) whose selection is written to matchClubId and read by GameViewModel.saveMatch(). When the selected club tracks standings, an Official Match Toggle + footer also appears, writing matchIsOfficial (AppStorageKeys.matchIsOfficial) — reset to true whenever no club is selected or the selected club has standings tracking off
  GameView.swift             — iOS live scoring: NavigationStack shell (toolbar, discard alert, ticker, point-banner/match-over overlays) shared across all 8 GameScreenStyles; ScoreSideData/GameHeaderData are the shared per-side/per-header data structs each style renders from. Owns the Depth style (default — gradient/glass ScoreView tiles, two big tap tiles, games header, timer, banners). onAppear/onDisappear flip AppDelegate.setOrientation for the landscape-only Scoreboard style (unconditional portrait restore on exit). Tap-only — NO Digital Crown, NO HealthKit workout. Toolbar's second button (gearshape.fill) presents InMatchSettingsView (#260), tint-matched to the End/Done button for Minimal-style contrast. Restyle of the Watch's
  GameScreenStyle.swift      — iOS-only: the 8-case GameScreenStyle enum (Depth/Split/Minimal/Blackbird/Matchstick/BirdsEye/Tug/Scoreboard) behind AppStorageKeys.gameScreenStyle, free for everyone (no isPremium/paywall gating unlike CourtTheme); isLandscape marks Scoreboard as the one landscape-native style. Also hosts GameScreenStylePickerView/GameScreenStyleThumbnail (#239 — pushed picker screen with a static mockup per style, since a plain List Picker flattens custom labels to text on iOS) and Color.blended(toward:by:), a manual RGB blend used to derive light theme-tinted accents — Color.mix(with:by:) needs iOS 18, this app targets 17
  GameScoreboardSplit.swift  — iOS-only: SplitScoreboard, the "Bold Split-Screen" GameScreenStyle — two full-bleed zones divided by a diagonal DiagonalSplit Shape; the currently-serving side's zone takes the full CourtTheme gradient, the other stays neutral dark, so the color itself shows who's serving
  GameScoreboardMinimal.swift — iOS-only: MinimalScoreboard, the "Minimal & Calm" GameScreenStyle — flat two-row layout, hairline divider, one theme-colored accent, subtle theme-tinted background wash (not just a flat neutral fill, so the chosen Court Theme is still visible even in this restrained style)
  GameScoreboardBlackbird.swift  — iOS-only: BlackbirdScoreboard, a broadcast-style GameScreenStyle — pure black field, a central horizontal scorebar (names + tabular-numeral scores + games pips), serve shown as a shape marker (not color, dim when not serving) so it stays colorblind-safe. Two equal-height BlackbirdHalfs meet at the exact vertical midpoint and each anchors its own score row to that seam, so the bar's visible divider IS the tap boundary; each half is one full-height target (a `tapLayer` under an `allowsHitTesting(false)` `visualLayer`) that flashes on tap. Never reintroduce a floating bar over separate tap zones — that swallowed center-screen taps and made the row read as the target
  GameScoreboardMatchstick.swift — iOS-only: MatchstickScoreboard, a skeuomorphic retro-LED-gym-scoreboard GameScreenStyle — glowing theme-colored digits drawn over a faint ghost digit to approximate unlit LED segments, rendered in DSEG7 Classic Bold (the app's first bundled custom font — SIL OFL licensed, license text at repo-root Fonts/DSEG-LICENSE.txt, registered via UIAppFonts in this target's Info.plist) so the ghost overlay reads as real unlit 7-segment housing rather than a generic font's numeral shape; each of the two digit positions renders as its own fixed-width slot (ledDigitSlot) instead of one padded string, since a font's space glyph isn't guaranteed as wide as its digit glyph (DSEG7's measurably isn't) — a lit "SERVE" lamp per side, games-won as lamp dots
  GameScoreboardBirdsEye.swift   — iOS-only: BirdsEyeScoreboard, a top-down court-diagram GameScreenStyle — the net line divides the two tap zones, and the serve marker sits in the correct service court (right box on an even score, left on odd) per real serving rules instead of being purely decorative; games-won render as baseline tally marks
  GameScoreboardTug.swift        — iOS-only: TugScoreboard, the one motion-first GameScreenStyle — the boundary between the two zones drags toward whoever's ahead on points and the leading numeral grows, so momentum reads before the digits do; score changes spring-bounce via contentTransition(.numericText()); honors accessibilityReduceMotion (pins the divider at the midpoint, swaps the bounce for a fade)
  GameScoreboardClassic.swift    — iOS-only (#252): ClassicScoreboard, the "Scoreboard" GameScreenStyle — manual courtside flip-card scoreboard (hinge-seam FlipCards on binder rings, games mini-card, name plates, theme base rail; card-flip on each point, skipped under Reduce Motion) and the app's ONE landscape screen: GameView flips AppDelegate.orientationLock to landscape while it's on screen. Home/"me" left, visitor right. Blackbird's tapLayer/visualLayer contract
  GameViewModel.swift        — iOS @MainActor VM; per-target adaptation of the Watch's (UIKit haptics, no WorkoutManager, top-level GameMode). Match-config keys (matchMyName/…/gameMode/matchIsOfficial) stay device-local so phone- and watch-scored matches can't collide mid-setup; finished MatchRecord flows through AppStore.saveHistory
  HapticsProvider.swift      — iOS HapticsProvider protocol + GameHapticType (platform-neutral) + UIKitHapticsProvider (impact/notification generators). Mirrors the Watch's WKHapticType abstraction
  StoreManager.swift / PaywallView.swift — iOS copies of the Watch's StoreKit 2 manager + a fuller storefront sheet (feature list, price buttons, restore); same entitlement gating call sites (StatsView, PlayerEditView, GameView theme fallback)
  InMatchSettingsView.swift  — #260: iOS restyle of the Watch's — NavigationStack + List sheet from GameView's toolbar gear button, Audio toggles + a Court Theme row pushing the existing CourtThemePickerView. No Crown Scoring row (Watch-only hardware, same as SettingsView)
  EraseDataView.swift        — #264: iOS restyle of the Watch's — pushed from SettingsView's Danger Zone section via a plain NavigationLink. Same type-to-confirm "DELETE" TextField and owned-clubs warning list. On success shows an alert with a "Done" button that calls `dismiss()`; the user's normal back-navigation to ContentView's root re-triggers the first-launch name prompt (no bound NavigationPath exists to force a pop-to-root programmatically, and none is needed)
  AdBannerView.swift         — iOS-only, the single home of ALL ad code (GoogleMobileAds v12 SPM dep on this target only): UMP consent → ATT prompt → SDK start → 320×50 banner; mounted via safeAreaInset on ContentView/HistoryView/StatsView behind Entitlements.showsAds, never on GameView. Test app/ad-unit ids until release
  CourtTheme.swift / AudioFeedback.swift — per-target copies of the Watch's (CourtTheme colors; AVFoundation tones + speech are cross-platform). CourtTheme.isPremium (both copies) gates red/black behind Pro/theme-pack; iOS copy also hosts CourtThemePickerView (#239, swatch-per-theme pushed picker — same UIMenu-flattening rationale as GameScreenStylePickerView)
  AppStore.swift             — iOS cache/singleton; port of the Watch's (save* enqueue through the same injected `syncEngine: SyncEngine`, applyRemoteUpsert/applyRemoteDeletions/applyRemoteSettings). Same backend-switch plumbing as the Watch's — mutable `syncEngine`, `activateSupabaseSync()`/`deactivateSupabaseSync()`, `currentSettingsSnapshot()`. Friends v1 (#7c): byte-identical friendRequests/friends/saveFriendRequests addition as the Watch's, incl. the friendActivity cache. `eraseAllData()` (#264) is a byte-identical port of the Watch's full-wipe orchestrator
  SupabaseSyncEngine.swift   — Roadmap Phase 9c: iOS port of the Watch's `SyncEngine` conformer AppStore swaps to when a device opts into Supabase; same `CloudSyncSpike`/`SupabaseSyncManager` adapter pattern, same serial-`Task`/batched-upsert design
  AppDelegate.swift          — Roadmap Phase 9f-3 removed `SceneDelegate.swift` entirely (its only job was CKShare-acceptance forwarding); `AppDelegate` no longer overrides `application(_:configurationForConnecting:options:)`, so iOS just uses its own default scene handling, which SwiftUI's `WindowGroup` lifecycle already expects. Installed via @UIApplicationDelegateAdaptor in badminton_score_trackerApp.swift. Activates WCSession to relay a signed-in Supabase session to the paired watch (`relaySessionToWatch`) — the phone always performs the actual Google OAuth handshake. Also owns the app-wide orientation lock (#252): static orientationLock read by supportedInterfaceOrientationsFor + setOrientation(_:) to force-rotate — portrait everywhere except GameView's landscape Scoreboard style (Info.plist declares landscape, this delegate gates it)
  SettingsView.swift         — iOS: paywall entry + Game Mode/Audio/Court Theme/Match Format/Timer, mirroring the Watch's SettingsView. Excludes Crown Scoring (no Digital Crown on iPhone) and the gesture-hint Controls screen (Watch-only content); Roster/Clubs/Friends stay their own rows on ContentView's menu rather than nesting here, since iOS navigation is push-based, not Settings-as-hub. The "Sync Backend" Supabase opt-in section sits above Danger Zone (Roadmap Phase 9c-3) — this is the target that actually performs Google Sign-In (`SupabaseSyncManager.shared.signInWithGoogle(presentationAnchor:)` via `ASWebAuthenticationSession`) and relays the resulting session to the paired Watch (`AppDelegate.relaySessionToWatch(tokens:)`) before calling `AppStore.shared.activateSupabaseSync()`. Danger Zone section (#264, EraseDataView.swift)
```

Views read shared state via `@EnvironmentObject var store: AppStore`; live-game state lives in `GameViewModel` (created by `GameView` as `@StateObject`).

## AppStorage Keys

All key strings are constants in `BadmintonCore.AppStorageKeys` — **read that file for the current list**; declare `@AppStorage(AppStorageKeys.x)`, never an inline literal, and add new keys there first. Typed defaults stay at the `@AppStorage` declaration sites.

## Git Workflow — MUST FOLLOW

- **Never commit directly to `main`** — all changes go through a PR
- Create a `feature/...` or `fix/...` branch for every change
- Every PR that adds or changes a feature **must also update `SPEC.md`**
- Every PR that changes project structure, architecture, models, or conventions **must also update `CLAUDE.md`**
- After merging a PR, always clean up without being asked (repo settings only allow squash merges — `allow_merge_commit`/`allow_rebase_merge` are both off, confirmed via `gh api repos/rinaba501/badminton-score-tracker`; `--merge` fails outright):
  ```
  gh pr merge <number> --squash --delete-branch
  git checkout main && git pull
  git remote prune origin
  ```
  A squash-merged branch's tip commit is never an ancestor of `main` (`git merge-base --is-ancestor` returns false even though the content landed) — don't mistake that for the branch being unmerged when doing manual cleanup; check the PR/commit title or trust that `origin/<branch>` was auto-deleted.
- Do not leave stale local or remote branches — one branch per PR, deleted on merge

### CI & review

Seven parallel jobs per PR (SwiftLint, core `swift test`, localization key sync ×2 — Watch + iOS tables are checked separately since their key sets legitimately diverge, Watch App build-for-testing, Complication build, iOS App build); green in **~4 min** — details, runtimes, and polling advice in [docs/ci.md](docs/ci.md). Before pushing: run `swiftlint` and `swift test --package-path BadmintonCore`; build locally before pushing SwiftUI changes when possible. **Use plan mode for anything touching `SupabaseSyncEngine`/`AppStore` (Watch OR iOS)** (real bugs in this codebase lived there — see docs/ci.md; dual-target writers to the same Supabase project; sync correctness can't be proven by CI, only a two-device test), and run `/code-review` before merging non-trivial PRs.

## Keeping the Docs Up-to-Date

- User-facing feature change → **`SPEC.md`** (and move closed issues from Open to Closed there, with the PR number)
- New file, model, AppStorage key, or architectural pattern → **`CLAUDE.md`**
- Build/run changes or new top-level doc → **`README.md`** (human-facing; keep short, link out)

A future session reading CLAUDE.md + SPEC.md should have a complete picture of the project. Other docs: `ROADMAP.md` (phased architecture plan + phase→issue map — consult before architectural work, update on phase-issue close), `docs/ci.md` (CI details + review guidance), `.github/` PR/issue templates (follow their shape), `docs/` App Store/privacy content (update only when their subject changes).

## Conventions

- SwiftUI only — no UIKit / WKInterfaceController
- Keep `body` small: pull sub-sections into computed `some View` properties and hoist non-trivial expressions into explicitly-typed helpers — the Swift type-checker times out on large view expressions. Debug builds warn at 100ms per expression (`-warn-long-expression-type-checking=100`); don't ignore that warning
- watchOS constraints: small screen, large tap targets, no keyboard (scribble/dictation only)
- `BadmintonMatch` must remain a pure value type — no UI, no timers, no side effects
- Platform-free logic goes in `BadmintonCore` (never imports SwiftUI/WatchKit); package API the app uses must be `public` (explicit inits included — an `internal` member fails the Watch App build). UI presentation of package models goes in app-side extensions (see `PlayerAvatar.swift`)
- Audio: tones via `AVAudioEngine`, speech via `AVSpeechSynthesizer` with `.duckOthers`; delay speech by tone duration
- Localization: every user-facing string goes in `Localizable.strings` for all 6 languages
- Sentinel identity (guests, "Me") is never a hardcoded literal: store/compare via `Player.guestNearToken`/`.guestFarToken`/`.isGuestName(_:)`/`.defaultMyName`; display/speak via `Player.displayName(for:)`/`.guestNearLabel`/`.guestFarLabel`. Tokens are stored, labels are rendered at the last moment — that's what keeps guest detection locale-independent
- Accessibility: custom/gesture controls need `accessibilityLabel`/`Hint` + traits; decorative imagery gets `accessibilityHidden(true)`; a11y strings are localized (`a11y.*` keys)
- Persistence: read/write `[Player]`/`[MatchRecord]` only through `PersistenceStore` — never inline `JSONEncoder`/`JSONDecoder` in views

## Token Economy

CLAUDE.md is loaded into every session — keep it terse when the doc-update rules require touching it:

- New entries here state *what exists, where it lives, and the hard rule* in one line. Deep rationale and design history go in doc comments at the top of the source file (loaded only when that file is read) or in `docs/`, never as paragraphs here
- Never read `project.pbxproj` (~34KB) or all 6 `Localizable.strings` files wholesale — grep for the section/key you need and edit surgically
- SPEC.md/ROADMAP.md/docs/ are read-on-demand: link to them, don't duplicate their content here
- If SPEC.md's Closed Issues table grows long, prune old rows — git history keeps the record

## GitHub Repo

`rinaba501/badminton-score-tracker` — https://github.com/rinaba501/badminton-score-tracker/issues
